import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  logoContainer: {flex: 0.2, alignItems: 'center', justifyContent: 'center'},
  btnContainer: (style = {flex: 0.1}),
});
export default styles;
